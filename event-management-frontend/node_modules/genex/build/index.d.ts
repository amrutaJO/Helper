import ret from 'ret';
declare class Genex {
    readonly tokens: ret.Root;
    readonly charset: number[];
    constructor(regex: string | RegExp, charset?: string);
    count(): number;
    generate(callback?: (value: string) => boolean | void): string[];
}
declare const _default: (regex: string | RegExp, charset?: string) => Genex;
export = _default;
